// This is only needed if you need to normalise nested model data using normalizr
// See normalizr docs for examples

// import { Schema } from 'normalizr';

// const {{ name }} = new Schema('{{ name }}', { idAttribute: 'id' });

// export default {{ name }};
